# 의료기관 데이터 추출 및 Supabase 업로드를 위한 필수 라이브러리
# Medical facility data extraction and Supabase upload requirements

# 기본 라이브러리
requests>=2.31.0
tabulate>=0.9.0

# Supabase 클라이언트
supabase>=2.0.0

# 선택적 의존성 (이미 설치되어 있을 수 있음)
# python-dotenv>=1.0.0  # 환경변수 파일 지원 

# Medical Data Collection Requirements
# 의료 데이터 수집을 위한 Python 패키지

# API 호출 및 데이터 처리
requests>=2.31.0

# Supabase 클라이언트
supabase>=2.0.0

# 환경 변수 관리
python-dotenv>=1.0.0

# 날짜/시간 처리 (표준 라이브러리에 포함)
# datetime (built-in)

# JSON 처리 (표준 라이브러리에 포함)
# json (built-in)

# 설치 방법:
# pip install -r requirements-medical-data.txt 